# Cloudflare / Wrangler local cache
.wrangler/

# OS junk
.DS_Store

# Node (if ever used)
node_modules/
npm-debug.log*

# Internal working files / brand source art (not served)
_preview/
brand/
images/hero-pardes.*

# Personal documents — never publish
מכתב-*
